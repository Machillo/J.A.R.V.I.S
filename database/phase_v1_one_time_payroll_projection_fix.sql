-- Fase V1 - Payroll extras no recurrentes.
-- No cambia estructura; solo deja documentada la regla del motor.
-- Después de desplegar backend/frontend, pulse "Recalcular estrategia" para guardar una guía nueva.

-- Verificación recomendada:
-- SELECT * FROM payroll_events ORDER BY created_at DESC LIMIT 10;
-- SELECT * FROM bonuses ORDER BY created_at DESC LIMIT 10;
