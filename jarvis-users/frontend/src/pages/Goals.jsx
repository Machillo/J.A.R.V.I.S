import { useEffect, useState } from "react";
import { createGoal, deleteGoal, getGoals } from "../services/jarvisApi";
const money=v=>new Intl.NumberFormat("es-CR",{style:"currency",currency:"CRC",maximumFractionDigits:0}).format(Number(v)||0);
export default function Goals(){const [rows,setRows]=useState([]);const [form,setForm]=useState({name:"",target_amount:"",current_amount:0,target_date:"",priority:"medium"});const load=()=>getGoals().then(setRows);useEffect(()=>{load()},[]);const submit=async e=>{e.preventDefault();await createGoal({...form,target_amount:Number(form.target_amount),current_amount:Number(form.current_amount||0),target_date:form.target_date||null});setForm({name:"",target_amount:"",current_amount:0,target_date:"",priority:"medium"});load()};return <section><div className="hero"><h1>Metas</h1></div><form className="panel form horizontal" onSubmit={submit}><input required placeholder="Meta" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="number" placeholder="Objetivo" value={form.target_amount} onChange={e=>setForm({...form,target_amount:e.target.value})}/><input type="date" value={form.target_date} onChange={e=>setForm({...form,target_date:e.target.value})}/><button>Agregar</button></form><div className="panel table">{rows.map(g=><div className="row" key={g.id}><span><strong>{g.name}</strong><small>{money(g.current_amount)} / {money(g.target_amount)}</small></span><button className="danger" onClick={async()=>{if(confirm("¿Eliminar meta?")){await deleteGoal(g.id);load()}}}>Eliminar</button></div>)}</div></section>}
