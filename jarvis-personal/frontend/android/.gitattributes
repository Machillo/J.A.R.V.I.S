# Files regenerated by `npx cap sync android` are written with LF on every OS.
# Pin them to LF so a sync on Windows (core.autocrlf=true) does not show phantom changes.
app/capacitor.build.gradle text eol=lf
capacitor.settings.gradle text eol=lf
