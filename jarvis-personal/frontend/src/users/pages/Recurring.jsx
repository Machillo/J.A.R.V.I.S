import {useEffect,useState} from "react";import{createRecurring,deleteRecurring,getRecurring,updateRecurring}from"../services/jarvisApi";
const money=v=>new Intl.NumberFormat("es-CR",{style:"currency",currency:"CRC",maximumFractionDigits:0}).format(Number(v)||0);const empty={name:"",amount:"",category:"general",item_type:"expense",frequency:"monthly",due_day:"",is_active:true};
export default function Recurring(){const[data,setData]=useState(null),[form,setForm]=useState(empty),[error,setError]=useState("");const load=()=>getRecurring().then(setData).catch(e=>setError(e.message));useEffect(load,[]);const submit=async e=>{e.preventDefault();try{await createRecurring({...form,amount:Number(form.amount),due_day:form.due_day?Number(form.due_day):null});setForm(empty);load()}catch(x){setError(x.message)}};const toggle=async x=>{await updateRecurring(x.id,{...x,is_active:!x.is_active});load()};return <section><div className="hero"><span>BASIC 06</span><h1>Recurrentes</h1><p>Servicios, suscripciones, alquiler y otros cobros repetidos.</p></div>{error&&<div className="panel error">{error}</div>}<form className="panel form" onSubmit={submit}><input required placeholder="Nombre" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input required type="number" min="0.01" placeholder="Monto" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/><input placeholder="Categoría" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/><select value={form.item_type} onChange={e=>setForm({...form,item_type:e.target.value})}><option value="expense">Gasto</option><option value="income">Ingreso</option></select><select value={form.frequency} onChange={e=>setForm({...form,frequency:e.target.value})}><option value="weekly">Semanal</option><option value="biweekly">Quincenal</option><option value="monthly">Mensual</option><option value="quarterly">Trimestral</option><option value="annual">Anual</option></select><input type="number" min="1" max="31" placeholder="Día de cobro" value={form.due_day} onChange={e=>setForm({...form,due_day:e.target.value})}/><button>Agregar recurrente</button></form>{data&&<><div className="kpis"><div className="card"><small>Costo mensual</small><strong>{money(data.monthly_expenses)}</strong></div><div className="card"><small>Costo anual</small><strong>{money(data.annual_expenses)}</strong></div></div><div className="panel table">{data.items.map(x=><div className={`row ${x.is_active?"":"muted-row"}`} key={x.id}><span><strong>{x.name}</strong><small>{money(x.monthly_amount)}/mes · {money(x.annual_amount)}/año · día {x.due_day||"—"}</small></span><span className="actions"><button onClick={()=>toggle(x)}>{x.is_active?"Pausar":"Activar"}</button><button className="danger" onClick={async()=>{if(confirm("¿Eliminar recurrente?")){await deleteRecurring(x.id);load()}}}>Eliminar</button></span></div>)}</div></>}</section>}
