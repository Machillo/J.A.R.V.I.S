import {useEffect,useState} from "react";
import {contributeGoal,createGoal,deleteGoal,getGoals,updateGoal} from "../services/jarvisApi";
const money=value=>new Intl.NumberFormat("es-CR",{style:"currency",currency:"CRC",maximumFractionDigits:0}).format(Number(value)||0);
const empty={name:"",target_amount:"",current_amount:0,target_date:"",priority:"medium",status:"active"};
const monthly=goal=>{if(!goal.target_date)return null;const now=new Date(),end=new Date(`${goal.target_date}T12:00:00`),months=Math.max((end.getFullYear()-now.getFullYear())*12+end.getMonth()-now.getMonth(),1);return Math.max((Number(goal.target_amount)-Number(goal.current_amount))/months,0);};
export default function Goals({plan="free"}){const advanced=plan!=="free",[rows,setRows]=useState([]),[form,setForm]=useState(empty),[edit,setEdit]=useState(null),[error,setError]=useState("");const run=async fn=>{setError("");try{return await fn();}catch(err){setError(err.message);return null;}};const load=()=>run(async()=>setRows(await getGoals()));useEffect(()=>{load();},[]);const payload=value=>({...value,target_amount:Number(value.target_amount),current_amount:Number(value.current_amount||0),target_date:value.target_date||null});const submit=async event=>{event.preventDefault();if(await run(()=>createGoal(payload(form)))){setForm(empty);load();}};const contribute=async id=>{const amount=prompt("Monto del aporte");if(amount&&await run(()=>contributeGoal(id,{amount:Number(amount)})))load();};const save=async event=>{event.preventDefault();if(await run(()=>updateGoal(edit.id,payload(edit)))){setEdit(null);load();}};const fields=(value,set)=><><input required placeholder="Nombre de la meta" value={value.name} onChange={e=>set({...value,name:e.target.value})}/><input required type="number" min="0.01" placeholder="Monto objetivo" value={value.target_amount} onChange={e=>set({...value,target_amount:e.target.value})}/>{advanced&&<><input type="number" min="0" placeholder="Ya ahorrado" value={value.current_amount} onChange={e=>set({...value,current_amount:e.target.value})}/><input type="date" value={value.target_date||""} onChange={e=>set({...value,target_date:e.target.value})}/><select value={value.priority} onChange={e=>set({...value,priority:e.target.value})}><option value="low">Baja</option><option value="medium">Media</option><option value="high">Alta</option><option value="critical">Prioritaria</option></select></>}</>;
return <section><div className="hero"><span>{advanced?"BASIC 04":"FREE 05"}</span><h1>Metas de ahorro</h1><p>{advanced?"Aportes y ritmo necesario para llegar a tiempo.":"Objetivo, aportes manuales y progreso, sin estrategia."}</p></div>{error&&<div className="panel error">{error}</div>}<form className="panel form" onSubmit={submit}>{fields(form,setForm)}<button>Agregar meta</button></form><div className="goal-grid">{rows.map(goal=>{const target=Math.max(Number(goal.target_amount)||1,1),current=Number(goal.current_amount)||0,progress=Math.min(current/target*100,100),needed=monthly(goal);return <article className="panel goal-card-basic" key={goal.id}><header><b>{goal.name}</b>{advanced&&<small>{goal.priority}</small>}</header><progress max="100" value={progress}/><p>{progress.toFixed(1)}% · {money(current)} de {money(target)}</p><p>Faltan {money(Math.max(target-current,0))}</p>{advanced&&<p>{needed==null?"Agregá fecha para calcular el aporte":`${money(needed)}/mes · ${money(needed/2)}/quincena`}</p>}<div className="actions"><button onClick={()=>contribute(goal.id)}>Registrar aporte</button>{advanced&&<button onClick={()=>setEdit({...goal})}>Editar</button>}<button className="danger" onClick={async()=>{if(confirm("¿Eliminar meta?")&&await run(()=>deleteGoal(goal.id)))load();}}>Eliminar</button></div></article>;})}</div>{edit&&<div className="modal-backdrop"><form className="panel form edit-modal" onSubmit={save}><h3>Editar meta</h3>{fields(edit,setEdit)}<select value={edit.status} onChange={e=>setEdit({...edit,status:e.target.value})}><option value="active">Activa</option><option value="paused">Pausada</option><option value="completed">Completada</option></select><button>Guardar cambios</button><button type="button" onClick={()=>setEdit(null)}>Cancelar</button></form></div>}</section>;}
